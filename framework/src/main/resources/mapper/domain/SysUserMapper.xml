<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="nobody.domain.mapper.SysUserMapper">

    <select id="selectSimpleById" resultType="java.util.HashMap">
        SELECT
            id,
            nickname,
            IFNULL(signature, '') AS signature
        FROM sys_user
        WHERE id = #{id}
          AND status = 1
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectByNickname" resultType="java.util.HashMap">
        SELECT
            id,
            nickname,
            password_hash         AS passwordHash,
            IFNULL(username, '')  AS level,
            IFNULL(signature, '') AS signature,
            status,
            deleted
        FROM sys_user
        WHERE nickname = #{nickname}
        LIMIT 1
    </select>

</mapper>
